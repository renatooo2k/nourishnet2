name: Flutter CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4

      - name: 🔧 Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.22.0"

      - name: 📦 Instalar dependências
        run: flutter pub get

      - name: 🔍 Analisar código (ignorar falhas)
        run: |
          echo "🔍 Executando análise estática do Flutter..."
          flutter analyze || true

      # ⚠️ Testes desativados temporariamente
      # - name: 🧪 Executar testes
      #   run: flutter test

      - name: 🏗️ Build do APK (modo debug)
        run: |
          echo "🏗️ Iniciando build do APK (modo debug)..."
          flutter build apk --debug

      - name: 📤 Publicar APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: nourishnet-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk

      - name: ✅ Conclusão
        run: echo "✅ Build Flutter finalizado com sucesso!"
